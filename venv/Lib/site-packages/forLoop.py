import sys

"""
this is a sample iteration operation.
If indent=False it will not perform indentation in the result and this is the default
behaviour of this script. and this is deafult to Faluse.
And the level=0 is describing that this param with a defalut value of "0". if
you not pass any value it will treated as '0'.
"""
def iterateLoop(the_list,indent=False,level=0,fh=sys.stdout):
    """ basic for loop to iterate a list """
    for each_item in the_list:
        """ checking if there are any sub lists are available in list"""
        if isinstance(each_item,list):
            """ if any sub list is available call iterateLoop() function"""
            iterateLoop(each_item,indent,level+1,fh)
        else:
            """ there is no sub list in particular index proceed here"""
            """ if indent is true then perform this action"""
            if indent:
                """ to print result with tab """
                for tab_stop in range(level):
                    """ multipling tab with level value"""
                    print("\t"*level,end='',file=fh)
            print(each_item,file=fh)



